exec swayidle -w \
         timeout {{ .idleTimeout }} 'swaylock -f -c {{ .lockColor }}' \
         timeout {{ .dpmsTimeout }} 'swaymsg "output * power off"' resume 'swaymsg "output * power on"' \
         before-sleep 'swaylock -f -c {{ .lockColor }}'
